// DOM query selectors for various elements in the createCheckpoint.html template:

const createCheckpointSelectors = {
    AllowOverwrite: '#allow-overwrite',
    BrowseButton: '#change-path',
    CheckpointNameInput: '#checkpointname',
    CloseButton: '#close',
    CombinedPath: '.combined-path',
    CreateButton: '#create',
    CurrentPath: '#current-path',
    ErrorMessage: '#error-message',
    FileDoesNotExistNotice: '#file-does-not-exist-notice',
    FileExistsWarning: '#file-exists-warning',
    MainForm: '#data-entry-view form',
    ResultForm: '#results-view form',
    ResultText: '.result-text',
    ViewDataEntry: '#data-entry-view',
    ViewResults: '#results-view',
};

export { createCheckpointSelectors };