// Names of events expected by the code running in invocationPanel.ts:

const invokeEvents = {
    Init: 'init',
    Invoke: 'invoke',
    Update: 'update',
    Dismiss: 'dismiss',
};

export { invokeEvents };